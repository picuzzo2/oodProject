@model CSFinder.Models.Message
@{
    ViewData["Title"] = "Notification";
    Layout = "_CompanyLayout";
}


<!DOCTYPE html>
<html>
<head>
    <style>

        .card-img1 {
            border-radius: 50%;
            margin: 3px;
            margin-left: 10%;
            width: 50px;
            height: 50px;
            border: 1px solid #ddd;
        }


        img {
            margin: 20px;
            width: 500px;
        }

        .card-img1 {
            border-radius: 50%;
            margin: 20px;
            border: 1px solid #ddd;
            padding: 5px;
            width: 150px;
            height: 150px;
        }



        button {
            border: 3px solid #22a6b3;
            border-radius: 10px;
        }

        * {
            box-sizing: border-box;
        }

            *
            body {
                font-family: Arial;
                margin: 0;
            }
        /* Column container */
        .row {
            display: flex;
            flex-wrap: wrap;
        }

        /* Main column */
        .main {
            flex: 70%;
            background-color: white;
            padding: 20px;
        }

        .box {
            width: 500px;
            height: 600px;
            background-color: #f1f1f1;
            margin-top: 10px;
            border-radius: 10px;
        }

        .student {
            margin-left: 180px;
            margin-top: -90px;
        }

        .header {
            margin-left: 90px;
        }

        p {
            margin: 0;
        }

        .column1 {
            float: left;
            width: 25%;
            padding-right: 10px;
            height: 50%;
            background-color: #f1f1f1;
            position: fixed;
            border-radius: 30px;
        }

        .column2 {
            float: left;
            width: 100%;
            padding-left: 35%;
            height: 100%;
        }

        .column1 {
            float: left;
            width: 25%;
            padding-right: 10px;
            height: 52%;
            background-color: #f1f1f1;
            position: fixed;
            border-radius: 30px;
        }

        .column2 {
            float: left;
            width: 100%;
            padding-left: 40%;
            height: 100%;
        }

        .open-button1 {
            background-color: #4682B4;
            color: white;
            padding: 9px;
            border: none;
            cursor: pointer;
            opacity: 0.8;
            margin-top: 15px;
            bottom: 23px;
            right: 28px;
            width: 120px;
            border-radius:10px;
            margin-right: 5%;
            text-decoration: none;
        }
        .open-button1:hover {
            color:white;
            background-color:#003366;
            text-decoration: none;
        }

        .open-button2 {
            background-color: #228B22;
            color: white;
            padding: 6px;
            border: none;
            cursor: pointer;
            opacity: 0.8;
            margin-top: 15px;
            bottom: 23px;
            right: 28px;
            width: 120px;
        }
            .open-button2:hover{
color:white;
            background-color:#006633;

            }
        /* The popup form - hidden by default */
        .form-popup {
            width: 380px;
            height: auto;
            display: none;
            background-color: #bdc3c7;
            bottom: 0;
            right: 15px;
            border: 3px solid #f1f1f1;
            z-index: 9;
            box-shadow: 5px 10px;
        }

        /* Add styles to the form container */
        .form-container {
            max-width: 300px;
            padding: 10px;
            background-color: #bdc3c7;
        }



            /* Set a style for the submit/login button */
            .form-container .btn {
                background-color: #4CAF50;
                color: white;
                padding: 16px 20px;
                border: none;
                cursor: pointer;
                width: 100%;
                margin-bottom: 10px;
                opacity: 0.8;
                margin-top: 2px;
                margin-left: 10px;
            }

            /* Add a red background color to the cancel button */
            .form-container .cancel {
                background-color:red;
                margin-top: 10px;
                width: 100px;
                margin-left:10%;
            }

            /* Add some hover effects to buttons */
            .form-container .btn:hover, .open-button:hover {
                opacity: 1;
            }

        input {
            position: relative;
            background: #dfe4ea;
            border: 3px solid #ced6e0;
            border-radius: 20px;
            margin-bottom: 3px;
            margin-left: 10%;
        }

        label {
            margin-left: 10%;
        }

        .date {
            position: relative;
            background: #dfe4ea;
            border: 3px solid #ced6e0;
            border-radius: 10px;
            margin-bottom: 3px;
        }
    </style>
</head>
<body>

    <!-- The flexible grid (content) -->
    <div class="row">
        <div class="column1" align='center'>
            <img class="card-img1" src="@ViewBag.user.ImgProfile" border="0" /><br />
            <p><b>@ViewBag.user.Name</b></p>
            <p><b>ที่อยู่ : </b>@ViewBag.user.Address</p>
            <p><b>โทร : </b>@ViewBag.user.Phone</p>
            <p><b>Email : </b>@ViewBag.userEmail</p>
        </div>

        <div class="column2">
            <div>
                @{ int i = 0;
                   int j = 1000;
                @foreach (StudentNameMatching sm in @ViewBag.StudentNameMatchingList)
                {
                    i++;
                    j++;
                    <div class="main">
                        <div class="box">
                            <div style="padding-left:10%;">
                                <img class="card-img1" src="@sm.student.ImgProfile" border="0" />
                                <p>ชื่อ: @sm.student.Name</p>
                                @Html.ActionLink("Student Detail", "GetStudent", "Public", new { id = sm.student.SID }, new { @class = "open-button1", target = "_blank" })
                                @if (sm.matching.Result == "Waiting for interview")
                                {

                                    string interview = "interview" + j;
                                    {
                                        int target = i;
                                        <a href="#@interview" class="btn btn-info" data-toggle="collapse">นัดสัมภาษณ์</a>
                                        <div id="@interview" class="collapse">
                                            <form id="@i" class="form-container">
                                                <label for="interview">วันที่นัดสัมภาษณ์</label>
                                                @Html.EditorFor(model => model.Date, new { type = "date" })<br>
                                                <label for="interview">รายละเอียดในการนัดสัมภาษณ์</label>
                                                @Html.TextAreaFor(model => model.Detail, null)

                                                <button type="button" class="btn cancel" onclick="confirm(@target,@ViewBag.user.CID, @sm.matching.SID, @sm.matching.MID)">ยืนยัน</button>
                                            </form>
                                        </div>
                                    }
                                }
                                else if (sm.matching.Result == "Waiting for interview result")
                                {
                                    int status = j;
                                    <form >

                                        <label >สถานะ : </label>
                                        <select id="@status">
                                            <option class="Wait" value="Wait_Status">Wait</option>
                                            <option class="Pass" value="Pass_Status">Passed</option>
                                            <option class="Failed" value="Failed_Status">Failed</option>
                                        </select>
                                        <button type="button" class="btn btn-dark" onclick="statusSubmit(@status,@ViewBag.user.CID, @sm.matching.SID, @sm.matching.MID)">ยืนยัน</button>
                                    </form>
                                    
                                }
                                else if (sm.matching.Result == "Company accepted")
                                {
                                    <div class="btn btn-danger">Waiting for student confirmation</div>
                                }
                                else if(sm.matching.Result == "Company rejected")
                                {
                                    <div class="btn btn-danger">Rejected</div>
                                }
                                else if(sm.matching.Result == "Student accepted")
                                {
                                    <div class="btn btn-danger">Student accepted</div>
                                }
                                else if(sm.matching.Result == "Student rejected")
                                {
                                    <div class="btn btn-danger">Student rejected</div>
                                }
                            </div>  
                        </div>
                    </div>

                }}
            </div>
        </div>
    </div>
</body>
</html>
<script>
    function confirm(target, from, to, MID) {
        
        var data = $("#" + target).serialize();
        var intday = $("#" + target).serializeArray()[0].value;
        var detail = $("#" + target).serializeArray()[1].value;
        var today = new Date();
        var todaytime = today.getFullYear() + '-' + ('0' + (today.getMonth() + 1)).slice(-2) + '-' + ('0' + today.getDate()).slice(-2) + "T" + today.getHours() + ":" + today.getMinutes();
        if (!compairTime(intday, todaytime))
        {
            alert("Please enter time that greater than today");
            return false;
        }
        if (detail == "")
        {
            alert("Please enter details");
            return false;
        }
        data = data + "&from=" + from;
        data = data + "&to=" + to;
        data = data + "&MID=" + MID;
        $.ajax({
            type: "POST",
            data: data ,
            url: "/Company/InterviewAppointment",
            success(response) {
                alert(response);
                window.location.href = "/Company/Notification";
            }
        })
    }
    function compairTime(n1, n2) {
        if (parseInt(n1.substr(0, 3)) < parseInt(n2.substr(0, 3)))
            return false;
        else if (parseInt(n1.substr(0, 3)) > parseInt(n2.substr(0, 3)))
            return true;
        if (parseInt(n1.substr(5, 6)) < parseInt(n2.substr(5, 6)))
            return false;
        else if (parseInt(n1.substr(5, 6)) > parseInt(n2.substr(5, 6)))
            return true;
        if (parseInt(n1.substr(8, 9)) < parseInt(n2.substr(8, 9)))
            return false;
        else if (parseInt(n1.substr(8, 9)) > parseInt(n2.substr(8, 9)))
            return true;
        return false;
    }

    function statusSubmit(target, from, to, MID)
    {
        var data = $("#" + target + " :selected").val();
        var message = "";
        if (data.toString() == "Wait_Status") { return false; }
        else {
             message = message + "Detail=" + data + "&from=" + from + "&to=" + to + "&MID=" + MID;
        }
        $.ajax({
            type: "POST",
            data: message,
            url: "/Company/InterviewConfirmation",
            success(response) {
                alert(response);
                window.location.href = "/Company/Notification";
            }
        })
       
        debugger
    }
</script>
